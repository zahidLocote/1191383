@model IEnumerable<practica14.Models.DetalleVentum>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Cantidad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PrecioUnitario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Subtotal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Producto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Venta)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Cantidad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PrecioUnitario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Subtotal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Producto.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Venta.VentaId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DetalleVentaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DetalleVentaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DetalleVentaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
